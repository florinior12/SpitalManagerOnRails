<tr>
  <td> <%= link_to Medic.find(consult.medic_id).nume + ' ' + Medic.find(consult.medic_id).prenume, Medic.find(consult.medic_id) %> </td>
  <td> <%= link_to Pacient.find(consult.pacient_id).nume + ' ' + Pacient.find(consult.pacient_id).prenume, Pacient.find(consult.pacient_id) %> </td>
  <td> <%= consult.diagnostic  %> </td>
  <td> <%= consult.data  %> </td>
 <td><%= link_to 'Modifică ', edit_consult_path(consult)%></td>
  <td> <%= link_to "Şterge", consult, method: :delete, data: { confirm: "Ştergeţi consultaţia: " + consult.medic_id.to_s + ' ' + consult.pacient_id.to_s + "?" } %></td>

</tr>